data <- read.csv(file="dataset.csv", sep=",", head=TRUE)
data <- read.csv("dataset.csv") # mes rapid si es csv per comes

data_xlsx <- read_excel("dataset.xlsx") # si tenim excel
# si es necessari fer us de separate() del package tidyr

head(data) # per veure els primers / data # per veure tot

data[x,y] # x - fila / y - columna
data$col_name

data[5:10, c(2,4)] # Accés columnes 2 i 4 del rang de files 5 a la 10

summary(data) # resum de les dades (min, max, avg... de cada columna)
str(data) # estrucutra de les dades (cada columna quin tipus de dades és, int, char...)
class() # identificar etiqueta o categoria basica de l'objecte

mean(data&col_name) # mitjana aritmètica de la col_name
mean(data[1:50,]&col_name) # mitjana aritmetica de col_name files 1 a la 50

median() # mediana
var() # variance
sd() # standard deviation

plot(data[,]) # visualitzacio grafica tot el dataset
plot(data[,3:4]) # """ col 3 i 4

hist(data$petal.length[1:50], xlab="Petal length", ylab="Frequency", main="Petal length in Iris Setosa", col="blue") # exemple histograma


