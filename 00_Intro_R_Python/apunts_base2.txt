dataset_xlsx$Col_name <- as.numeric(dataset_xlsx$Col_name) # convertir columnes a numeriques per poder operar

var_name <- X # assignem valor X a var_name

# if / elseif / else 
if (cond1) {
	do_smth()
} else if (cond 2) {
	do_smth2()
} else {
	do_smth3()
}

# for
for (Col_name in dataset$Col_name) {
	do_smth1()
}

# while
while (i <= 5) {
	do_smth1()
}

# apply - per aplicar una funció al llarg de files o columnes dun dataset (sense haver de fer un for)
apply(dataset, MARGIN, FUNCTION)
apply_obj <- apply(Customers_csv["Age"], 2, sum); apply_obj
# sumem tots els valors de la columna Age 

# lapply - aplica una funció a cada element d'una llista retornant una llista com a resultat
lapply(dataset, FUNCTION)
lapply_obj <- lapply(Customers_csv[, c("Age", "Work.Experience")], mean); lapply_obj
# calcular la mitjana de les columnes Age i WorkExperience - retorna una llista on cada element es el resultat de la columna corresponent

